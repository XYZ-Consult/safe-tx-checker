<!doctype html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%= title %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <style>
      label{display:inline-block;width:100px;text-align:right;font-weight:bold;padding:5px}
      input[type=text]{width:400px}
      div.output{padding:10px}
      #result{color:red}
    </style>
    <% const app = require('./index.js') %>
  </head>
  <body onload="App(document)">
    <div id="app">
    </div>
  </body>
</html>
